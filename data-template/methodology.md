---
# PTE Methodology — rule库
#
# 由 pte skill 写入，由 english-immersion 在 English Mode ON 时被动读取。
# 每条规则在你日常英语输入中被自发命中 ≥ 3 次 → 状态改为 mastered。
#
# 添加方式：
#   - `/pte feed <文稿>` — Claude 从文稿提取后追加
#   - `/pte rule <规则>` — 手动口述一条，Claude 格式化后追加
---

rules: []
